<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Tlumach.Sample.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tlumach.Sample.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tru="using:Tlumach.Sample"
    mc:Ignorable="d"
    Title="WinUI sample for Tlumach">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Bottom" HorizontalAlignment="Center" Text="{x:Bind tru:Strings.Hello.CurrentValue, Mode=OneWay}" />
        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" HorizontalAlignment="Center"  Text="{x:Bind tru:Strings.Welcome.CurrentValue, Mode=OneWay}" />
        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
            <ComboBox Name="LanguageSelector" SelectionChanged="LanguageSelector_SelectionChanged" />
            <TextBlock Name="CopyrightLabel" Margin="16,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text=" | "  VerticalAlignment="Center"/>
            <TextBlock Text="{x:Bind tru:Strings.CultureInfo.CurrentValue, Mode=OneWay}" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
